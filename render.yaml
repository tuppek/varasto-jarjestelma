services:
  - type: web
    name: varasto-jarjestelma
    runtime: docker
    plan: free
    healthCheckPath: /api/health
    envVars:
      - key: AUTO_SEED
        value: "1"
      - key: DATA_DIR
        value: /data
